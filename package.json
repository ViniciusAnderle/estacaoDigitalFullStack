{
  "dependencies": {
    "express": "^4.18.2",
    "js-cookie": "^3.0.5"
  },
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "v18.16.0"
  }
}
